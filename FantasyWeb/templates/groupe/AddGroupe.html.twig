{% extends 'User/User.html.twig' %}
{% block bodyakhel %}
    <div class="tg-banner tg-haslayout">
        <div class="tg-imglayer">
            <img src="{{asset('images/bg-pattran.png')}}" alt="image desctription">
        </div>
        <div class="container">
            <div class="row">
                <div class="tg-banner-content tg-haslayout">
                    <div class="tg-pagetitle">
                        <h1>Ajouter un groupe</h1>
                    </div>
                    <ol class="tg-breadcrumb">

                        <li class="active">Ajouter un groupe</li>
                    </ol>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
{% block body %}

    <main id="tg-main" class="tg-main tg-haslayout">
        <!--************************************
                Top Rated Player Start
        *************************************-->

        <section class="tg-main-section tg-haslayout">
            <div class="container">
                <div class="tg-section-name">
                    <h2>Ajouter un groupe</h2>
                </div>
            </div>
            <div class="col-sm-push-4 col-xs-11 pull-right">
                    <div class="row">
                        <h6 style="color: firebrick">Nom équipe</h6>
                        <div class="form-group">
                        {{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}
                        {{ form_errors(form.nomGroupe) }}
                        {{ form_widget(form.nomGroupe, { 'attr': {'class': 'single-input','placeholder': ' Nom Groupe'} }) }}
                        {{ form_widget(form.Submit, { 'attr': {'class': 'tg-btn'} }) }}
                        {{ form_end(form) }}
                        </div>
                    </div>
            </div>
        </section>
    </main>

{#{{ form_start(form) }}#}
{#{{ form_widget(form) }}#}
{#    <button class="tg-btn" >Ajouter</div>#}
{#{{ form_end(form) }}#}

{% endblock %}