<form method="post" action="{{ path('actualites_delete', {'idActualites': actualite.idActualites}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ actualite.idActualites) }}">
    <button class="btn">Supprimer</button>
</form>
